// Vercel replaces this file during build using that project's environment variables.
// Do not hardcode production or test keys here, otherwise both apps can point to the same database.
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
