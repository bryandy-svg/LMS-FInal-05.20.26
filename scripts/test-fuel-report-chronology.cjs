const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const s=fs.readFileSync('supabase-app/app.js','utf8');const c=vm.createContext({truckingTimeMinutes:value=>{if(!value)return NaN;const [h,m]=value.split(':').map(Number);return h*60+m;}});let start=s.indexOf('function truckingFuelReportRows('),end=s.indexOf('\n}',start);vm.runInContext(s.slice(start,end+2),c);const row={report_no:'F1',driver_name:'Driver',fuel_date:'2026-09-14',company:'Customer',jobsite:'Site',time_in:'08:00',time_out:'08:30',gallons:10,fuel_selling_value:100,fuel_cost:40,has_fuel_rate:true};const reports=c.truckingFuelReportRows([row,{...row,time_in:'08:30',time_out:'09:00'},{...row,report_no:'F2',time_in:'13:00',time_out:'13:30'}],[{name:'Driver',hourly_labor_rate:20}]);assert.equal(reports.length,2);assert.equal(reports[0].run_hours,1);assert.equal(reports[0].total_labor_cost,20);assert.equal(reports[0].ticket_income,200);assert.equal(reports[0].fuel_cost,80);assert.equal(reports[0].income_less_labor,100);assert.equal(reports[1].run_hours,0.5);assert.equal(c.truckingFuelReportRows([{...row,time_in:'23:30',time_out:'00:30'}],[])[0].run_hours,1);console.log('PASS: grouping, report hours, saved driver rate, income, cost, profit, separate report gaps and overnight time.');
